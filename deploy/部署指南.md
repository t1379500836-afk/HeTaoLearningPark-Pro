# HeTaoLearningPark-Pro 部署指南

从零开始，在一台全新服务器上完成全部部署。

---

## 一、安装宝塔面板

```bash
# CentOS
yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && sh install.sh ed8484bec

# Ubuntu / Debian
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh ed8484bec
```

安装完成后终端会显示：
- 宝塔面板地址（如 `http://IP:8888/xxxx`）
- 用户名和密码

登录宝塔面板，按提示安装 **Nginx** 和 **MySQL 5.7+**（编译安装或极速安装均可）。

---

## 二、安装 Node.js

在宝塔终端执行：

```bash
source /www/server/nvm/nvm.sh
nvm install 18 && nvm use 18 && nvm alias default 18
node -v   # 确认输出 v18.x.x
```

---

## 三、创建数据库

宝塔面板 → **数据库** → **添加数据库**：

| 字段 | 填写 |
|------|------|
| 数据库名 | `hetao_learning` |
| 用户名 | `hetao_learning`（默认与数据库名相同） |
| 密码 | 自己设置，记住它 |

---

## 四、上传项目文件

### 4.1 本地构建前端

在本地项目根目录执行：

```bash
# 构建用户端
npm install
npm run build
# 产出 dist/ 目录

# 构建管理后台
cd admin
npm install
npm run build
# 产出 admin/dist/ 目录
```

### 4.2 上传到服务器

通过宝塔 **文件管理器** 或 scp，上传以下内容到 `/www/wwwroot/hetao/`：

```
/www/wwwroot/hetao/
├── dist/                      # 用户端构建产物
├── admin/dist/                # 管理后台构建产物
├── server/                    # 后端代码（不要上传 node_modules）
│   ├── index.js
│   ├── db.js
│   ├── package.json
│   ├── .env                   # 需要手动创建，见下一步
│   ├── middleware/
│   └── routes/
├── ecosystem.config.cjs       # PM2 配置
└── deploy/                    # 部署相关文件（可选）
```

> 注意：`server/node_modules` 不要上传，在服务器上安装。

---

## 五、配置后端

### 5.1 安装依赖

```bash
cd /www/wwwroot/hetao/server
npm config set registry https://registry.npmmirror.com/
npm install --omit=dev
```

### 5.2 创建环境配置

创建 `/www/wwwroot/hetao/server/.env`，内容如下（根据实际情况填写）：

```env
# 服务端口
PORT=3000

# MySQL（填写第三步创建的数据库信息）
DB_HOST=localhost
DB_PORT=3306
DB_USER=hetao_learning
DB_PASSWORD=你设置的数据库密码
DB_NAME=hetao_learning

# JWT 密钥（随便填一个长字符串）
JWT_SECRET=hetao_learning_jwt_secret_2026
```

---

## 六、启动后端

```bash
cd /www/wwwroot/hetao
npm install -g pm2
pm2 start ecosystem.config.cjs
pm2 save
pm2 startup    # 设置开机自启，按提示执行输出的命令
```

验证是否成功：

```bash
pm2 logs hetao-api --lines 10 --nostream
```

应看到：
```
数据库初始化完成
后端服务已启动: http://localhost:3000
```

---

## 七、配置 Nginx

### 7.1 添加站点

宝塔面板 → **网站** → **添加站点**：
- 域名：填写你的服务器 IP（如 `103.193.173.202`）
- 根目录：`/www/wwwroot/hetao`
- PHP：选 **纯静态**
- 点击提交

### 7.2 替换配置文件

点击刚创建的站点 → **设置** → **配置文件**，将内容全部替换为：

```nginx
server {
    listen 8080;
    server_name _;

    # 前端主站
    location / {
        root /www/wwwroot/hetao/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 管理后台
    location /admin {
        alias /www/wwwroot/hetao/admin/dist;
        index index.html;
        try_files $uri $uri/ /admin/index.html;
    }

    # API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    access_log /www/wwwlogs/hetao.log;
    error_log /www/wwwlogs/hetao.error.log;
}
```

保存后 Nginx 会自动重载。

---

## 八、放行端口

需要在 **两个地方** 都放行 8080 端口：

### 8.1 宝塔防火墙

宝塔面板 → **安全** → 放行 `8080`

### 8.2 云服务商安全组

登录云服务商控制台（阿里云/腾讯云/华为云等）：
- 找到该服务器的 **安全组**
- 添加入站规则：TCP 协议，端口 `8080`，源地址 `0.0.0.0/0`

---

## 九、验证

| 地址 | 页面 | 说明 |
|------|------|------|
| `http://IP:8080/` | 用户端 | 学员访问 |
| `http://IP:8080/admin` | 管理后台 | 账号 `admin` / `admin123` |

---

## 常用运维命令

```bash
pm2 status                           # 查看后端状态
pm2 restart hetao-api                # 重启后端
pm2 logs hetao-api --lines 20 --nostream  # 查看日志（不追踪）
nginx -t                             # 检查 Nginx 配置
nginx -s reload                      # 重载 Nginx
```

---

## 更新部署

当代码有更新时：

```bash
# 1. 在本地重新构建
npm run build
cd admin && npm run build

# 2. 上传新的 dist/ 和 admin/dist/ 到服务器（覆盖）

# 3. 如果后端代码有变动，上传后重启
pm2 restart hetao-api
```
