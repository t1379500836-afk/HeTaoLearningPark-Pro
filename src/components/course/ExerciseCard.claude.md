# ExerciseCard.vue - 习题卡片组件

## 功能特性

- **选择题模式**：支持单选题（A/B/C/D等选项）
- **即时反馈**：选择后立即显示正确/错误状态
- **提示系统**：点击显示提示（答题前）
- **解析展示**：答题后显示详细解析
- **数学关联**：显示题目关联的数学概念
- **Markdown支持**：题目和解析支持代码块和行内代码

---

## Props API

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `exercise` | Object | **必填** | 习题数据对象 |

### exercise 数据结构

```javascript
{
  id: 'ex-1',
  level: 'easy',              // 难度：easy/medium/hard
  levelLabel: '基础',
  levelEmoji: '🟢',
  type: 'multiple-choice',    // 题型：单选
  mathConcept: '数数',        // 关联的数学概念
  question: '...',            // 题目（支持Markdown）
  options: [                  // 选项数组
    'A. 选项内容',
    'B. 选项内容',
    'C. 选项内容',
    'D. 选项内容'
  ],
  answer: 0,                  // 正确答案索引（0-based）
  explanation: '...',         // 解析（支持Markdown）
  hint: '...'                 // 提示内容
}
```

---

## Events

无自定义事件。

---

## 使用示例

```vue
<template>
  <ExerciseCard :exercise="exerciseData" />
</template>

<script setup>
import ExerciseCard from '@/components/course/ExerciseCard.vue'

const exerciseData = {
  id: 'ex-1',
  level: 'easy',
  levelLabel: '基础',
  levelEmoji: '🟢',
  type: 'multiple-choice',
  mathConcept: '数数',
  question: '以下哪个是正确的变量名？',
  options: [
    'A. `2variable`',
    'B. `my_var`',
    'C. `class`',
    'D. `my-var`'
  ],
  answer: 1,
  explanation: '变量名规则：...\n```python\nmy_var = 10\n```',
  hint: '变量名不能以数字开头'
}
</script>
```

---

## 代码位置

- [ExerciseCard.vue:69-89](src/components/course/ExerciseCard.vue#L69-L89) - Props和computed
- [ExerciseCard.vue:91-114](src/components/course/ExerciseCard.vue#L91-L114) - 选项格式化和Markdown处理
- [ExerciseCard.vue:116-136](src/components/course/ExerciseCard.vue#L116-L136) - 选项状态计算和选择逻辑

---

## 题目状态

| 状态 | 说明 | 样式 |
|------|------|------|
| 未选择 | 选项可点击 | 白色背景，灰色边框 |
| 已选择（正确） | 显示正确答案 | 绿色背景 |
| 已选择（错误） | 显示错误选择 | 橙色背景 |
| 正确答案显示 | 高亮正确选项 | 绿色背景 |

---

## Markdown支持

支持的Markdown语法：
- 代码块：` ```python ... ``` `
- 行内代码：`` `code` ``
- 换行：`\n`

---

## 响应式设计

| 断点 | 变化 |
|------|------|
| ≤ 768px | 卡片内边距减小，选项标签字号减小 |

---

## 已知问题 & 待办

- [ ] 支持更多题型（填空题、判断题）
- [ ] 添加答题统计功能
- [ ] 添加"收藏题目"功能
